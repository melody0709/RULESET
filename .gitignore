# ============================================================================
# 本仓库是 GitHub 公开仓库 —— 只上传「要发布的规则集」，以下内容一律不上传。
#
# 注意：.gitignore 只对「未跟踪」的文件生效。已经 commit 过的文件不会因此消失，
#       如果误提交过，需要 git rm --cached <file> 才会从仓库里移除。
#
# 本仓库只应包含 ruleset_self_*.yaml（各区域规则集）。
# 除此之外的任何东西——尤其是分析文档与助手记忆——都不属于这里。
# ============================================================================

# ---- 1. 本地运行产物 / 缓存 -------------------------------------------------
# 在本目录用 mihomo -d . 跑过一次就会生成，动辄几十 MB，绝不能进公开仓库。
# GEO 数据库（GeoSite.dat / GeoIP.dat / Country.mmdb 都是几十 MB 级）
*.dat
*.mmdb
# provider 本地缓存（订阅内容，含节点地址与凭证）
proxy_provider/
rule_provider/
providers/
# 内核运行时文件
cache.db
*.log
*.pid
# 规则集快照 / 调试临时导出
*.mrs
A.json
B.json
p.json
proxies.json

# ---- 2. 编辑器 / 系统垃圾 ---------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# ---- 3. 本地工作区（助手记忆与缓存，不进公开仓库）--------------------------
.workbuddy/

# ---- 4. 内部分析/方案文档（含判断依据与链路细节，不公开）--------------------
# 这些是本地工作文档，别 git add 进公开仓库。
plan-*.md
impl-*.md
audit-*.md
_diff.tmp
*.bak-*
# ---- 以上规则结束 -----------------------------------------------------------
